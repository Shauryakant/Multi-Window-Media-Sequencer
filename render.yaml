services:
  - type: web
    name: media-sequencer-backend
    env: docker
    dockerfilePath: Dockerfile
    plan: free
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 8080
      - key: SYNC_DURATION_SECONDS
        value: 5
      - key: MONGO_URI
        sync: false
